import { join } from 'path';

export const UPLOAD_PATH = join(__dirname, '..', '..', 'uploads');
export const VIEW_PATH = join(__dirname, '..', '..', 'views');

console.log('UPLOAD_PATH', UPLOAD_PATH);
console.log('VIEW_PATH', VIEW_PATH);
